{
  "scripts": {
    "install:backend-deps": "venv\\Scripts\\python -m pip install -U \"shapely>=2.0.0\"",
    "dev": "npm run dev --prefix frontend-react",
    "dev:backend": "venv\\Scripts\\python -m uvicorn app:app --host 127.0.0.1 --port 8000 --reload",
    "restart:backend": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/restart-backend.ps1",
    "build": "npm run build --prefix frontend-react",
    "preview": "npm run preview --prefix frontend-react"
  },
  "devDependencies": {
    "@types/node": "^25.5.0"
  }
}
